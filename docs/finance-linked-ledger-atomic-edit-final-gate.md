# Finance linked-ledger atomic edit final gate

Merge only when the latest branch head has a READY Vercel preview and final PR review has no actionable findings. After merge, verify the production deployment for the squash commit reaches READY on `erp.luminalfactory.com`.
