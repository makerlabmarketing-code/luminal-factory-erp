# Activation boundary

The database RPC is already deployed but cannot be called by `anon` or `authenticated`. Production application activation occurs only when the owning application PR is merged and its Vercel production deployment reaches READY.
