# Review checklist

Review the final PR as one slice. Confirm server authorization precedes the service-role RPC, browser roles cannot execute the RPC, ambiguous/target-conflict counter states fail closed, no smoke rows persisted, and unrelated ERP modules are unchanged.
